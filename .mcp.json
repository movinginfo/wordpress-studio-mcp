{
  "mcpServers": {
    "wordpress-studio-ext": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "wordpress-studio-mcp@latest"],
      "env": {
        "STUDIO_HOME": "${STUDIO_HOME}",
        "STUDIO_SITES_ROOT": "${STUDIO_SITES_ROOT}"
      }
    }
  }
}
